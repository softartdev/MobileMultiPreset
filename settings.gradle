pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        google()
        jcenter()
    }
}
rootProject.name = 'MobileMultiPreset'

include(':shared')
include(':androidApp')
